# Unit-проверки

| Требование или правило | Что проверяем изолированно | Вход | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| `SEC-1` | Маскирование секретов перед промптом | diff с `token=test-123`, `password=x` | В `prompt` только `[REDACTED]`, секретов нет | Юнит-тест чистки, строка `review_service.py:20` |
| `API-1` | Отклонение длинного diff | diff длиной 20 001 символ | Отказ с указанием `413`, LLM не вызывается | Юнит-тест валидатора размера |
| `QA-1/OUT-1` | Риск без evidence отбрасывается | Кандидат-риск без строки diff и правила | Не попадает в `risks`, `len(risks) ≤ 3` | Юнит-тест фильтра + парсинг JSON |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): `P1-02`
- Что проверили и исправили сами: каждый кейс гонял в уме без сети — только чистка и валидатор; `len(risks) ≤ 3` добавил после того как `P1-01` вернул 6 пунктов
