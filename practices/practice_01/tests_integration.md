# Integration-проверки

| Связь компонентов | Что может сломаться | Как воспроизводим | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| `api.py` → `review_service.py` | `payload["diff"]` бросает `KeyError` → 500 | `POST /api/reviews` с `{}` и `{"diff": 123}` | `400/422`, без вызова LLM | Интеграционный тест endpoint, строки `api.py:36-37` |
| `review_service.py` → внешний LLM | Timeout/исключение роняет запрос | Мок `generate` с `TimeoutError` и задержкой 30s | Контролируемый ответ по `REL-1`, без зависания | Интеграционный тест с моком, строка `review_service.py:21` |
| Сервис → лог | Утечка diff/ответа в лог | Вызов с тестовым diff, чтение лога | Только `request_id`, длительность, статус (`OBS-1`) | Проверка лога |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): `P1-02`
- Что проверили и исправили сами: стык `api→service` взял из `api.py:36-37`, а не из головы; секреты в тестах только `test-123`, боевых ключей нет; `OBS-1` чуть не потерял — дописал третью строку отдельно
